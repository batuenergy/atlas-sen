shapely>=2.0
openpyxl>=3.1     # build_zonas_de_carga.py — parse the CENACE NodosP catálogo (xlsx)
requests>=2.31
ruff>=0.5
pytest>=8.0
mcp>=1.2          # atlas_mcp.py — natural-language access to the dataset
